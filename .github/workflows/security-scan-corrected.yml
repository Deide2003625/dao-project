name: 🔒 Security Hardening – Fix ZAP Vulnerabilities

on:
  workflow_dispatch:
  push:
    branches: [main, develop, feature/Docker_v2]
  pull_request:
    branches: [main]

# ══════════════════════════════════════════════════════════════
# SOLUTION A – Permissions déclarées au niveau du workflow
# Accorde les droits d'écriture au GITHUB_TOKEN par défaut.
# ✅ Fonctionne pour les dépôts personnels et la plupart des orgs.
#
# Si vous avez encore l'erreur 403 → voir SOLUTION B (PAT)
# dans les commentaires de chaque step checkout ci-dessous.
# ══════════════════════════════════════════════════════════════
permissions:
  contents: write       # autorise git push par github-actions[bot]
  pull-requests: write  # autorise les commentaires de PR

jobs:
  # ─────────────────────────────────────────────
  # JOB 1 – AUDIT & PATCH npm dependencies
  # ─────────────────────────────────────────────
  audit-and-patch:
    name: 📦 Audit & Patch Dependencies
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # SOLUTION A : token par défaut (nécessite permissions: contents: write ci-dessus)
          token: ${{ secrets.GITHUB_TOKEN }}
          # SOLUTION B : si 403 persiste malgré les permissions ci-dessus
          # 1. Settings → Developer settings → Personal access tokens → Classic
          # 2. Scope requis : repo (full)
          # 3. Ajoutez-le dans Settings → Secrets → Actions → New secret → nom : GH_PAT
          # 4. Remplacez la ligne token ci-dessus par :
          # token: ${{ secrets.GH_PAT }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run npm audit and auto-fix
        run: npm audit fix --force || true

      - name: Commit patched package-lock.json
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 'fix(security): npm audit fix – patch vulnerable dependencies'
          file_pattern: 'package*.json'

  # ─────────────────────────────────────────────
  # JOB 2 – SÉCURISATION DES EN-TÊTES HTTP
  # Corrige : CSP, X-Frame-Options, COEP, COOP, CORP,
  #           Permissions-Policy, X-Content-Type-Options, X-Powered-By
  # ─────────────────────────────────────────────
  harden-http-headers:
    name: 🛡️ Harden HTTP Security Headers (Next.js)
    runs-on: ubuntu-latest
    needs: audit-and-patch
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          # token: ${{ secrets.GH_PAT }}   ← Solution B

      - name: Remove accidental .bak file (committed in previous run)
        run: |
          git rm --cached next.config.js.bak 2>/dev/null || true
          git rm --cached next.config.ts.bak 2>/dev/null || true
          rm -f next.config.js.bak next.config.ts.bak

      - name: Apply security headers to next.config
        run: |
          node << 'EOF'
          const fs = require('fs');
          const configFile = fs.existsSync('next.config.ts') ? 'next.config.ts' : 'next.config.js';
          let content = fs.existsSync(configFile) ? fs.readFileSync(configFile, 'utf8') : '';

          const headersBlock = `
            async headers() {
              const securityHeaders = [
                { key: 'Content-Security-Policy',      value: "default-src 'self'; script-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net https://code.jquery.com; style-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com https://cdn.jsdelivr.net; img-src 'self' data: blob:; connect-src 'self'; frame-ancestors 'none'; object-src 'none'; base-uri 'self'; form-action 'self'" },
                { key: 'X-Frame-Options',              value: 'DENY' },
                { key: 'Cross-Origin-Embedder-Policy', value: 'require-corp' },
                { key: 'Cross-Origin-Opener-Policy',   value: 'same-origin' },
                { key: 'Cross-Origin-Resource-Policy', value: 'same-origin' },
                { key: 'Permissions-Policy',           value: 'camera=(), microphone=(), geolocation=(), payment=(), usb=()' },
                { key: 'X-Content-Type-Options',       value: 'nosniff' },
                { key: 'Strict-Transport-Security',    value: 'max-age=63072000; includeSubDomains; preload' },
                { key: 'Referrer-Policy',              value: 'strict-origin-when-cross-origin' },
              ];
              return [{ source: '/(.*)', headers: securityHeaders }];
            },`;

          if (content.includes('nextConfig')) {
            if (!content.includes('poweredByHeader'))
              content = content.replace(/const nextConfig[^=]*=\s*\{/, 'const nextConfig = {\n  poweredByHeader: false,');
            if (!content.includes('async headers'))
              content = content.replace(/^(};?\s*(?:export default|module\.exports))/m, `${headersBlock}\n$1`);
          } else {
            content = `/** @type {import('next').NextConfig} */\nconst nextConfig = {\n  poweredByHeader: false,\n${headersBlock}\n};\nmodule.exports = nextConfig;\n`;
          }
          fs.writeFileSync(configFile, content);
          console.log('✅ next.config mis à jour');
          EOF

      - name: Commit next.config changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 'fix(security): add CSP, X-Frame-Options, COEP, COOP, CORP, Permissions-Policy, X-Content-Type-Options, disable X-Powered-By'
          file_pattern: 'next.config.*'
          # token: ${{ secrets.GH_PAT }}   ← Solution B

  # ─────────────────────────────────────────────
  # JOB 3 – SUBRESOURCE INTEGRITY
  # ─────────────────────────────────────────────
  subresource-integrity:
    name: 🔏 Add Subresource Integrity (SRI) Hashes
    runs-on: ubuntu-latest
    needs: harden-http-headers
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          # token: ${{ secrets.GH_PAT }}   ← Solution B

      - name: Compute SRI hashes & generate docs
        run: |
          mkdir -p docs
          sri() { curl -sL "$1" | openssl dgst -sha384 -binary | openssl base64 -A; }

          BS="sha384-$(sri 'https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js')"
          JQ="sha384-$(sri 'https://code.jquery.com/jquery-3.6.0.min.js')"
          MD="sha384-$(sri 'https://cdn.jsdelivr.net/npm/@mdi/font@7.2.96/css/materialdesignicons.min.css')"

          cat > docs/sri-hashes.md << MDEOF
          # Subresource Integrity (SRI) Hashes
          > Auto-généré – ne pas modifier manuellement.

          ## Bootstrap 5.3.0 JS
          \`\`\`html
          <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"
                  integrity="${BS}" crossorigin="anonymous"></script>
          \`\`\`

          ## jQuery 3.6.0
          \`\`\`html
          <script src="https://code.jquery.com/jquery-3.6.0.min.js"
                  integrity="${JQ}" crossorigin="anonymous"></script>
          \`\`\`

          ## Material Design Icons 7.2.96
          \`\`\`html
          <link rel="stylesheet"
                href="https://cdn.jsdelivr.net/npm/@mdi/font@7.2.96/css/materialdesignicons.min.css"
                integrity="${MD}" crossorigin="anonymous"/>
          \`\`\`

          > ⚠️ Google Fonts ne supporte pas SRI. Migrez vers \`next/font\` pour hébergement local.
          MDEOF
          echo "✅ SRI hashes documentés"

      - name: Commit SRI documentation
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 'fix(security): add SRI hashes for Bootstrap, jQuery, MDI'
          file_pattern: 'docs/sri-hashes.md'
          # token: ${{ secrets.GH_PAT }}   ← Solution B

  # ─────────────────────────────────────────────
  # JOB 4 – FILESYSTEM & PATH TRAVERSAL
  # ─────────────────────────────────────────────
  harden-filesystem:
    name: 🗂️ Harden Filesystem & Path Traversal
    runs-on: ubuntu-latest
    needs: harden-http-headers
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          # token: ${{ secrets.GH_PAT }}   ← Solution B

      - name: Create image validation middleware
        run: |
          mkdir -p src/middleware
          cat > src/middleware/validateImageParams.ts << 'EOF'
          /**
           * Anti-Path Traversal sur /_next/image
           * Corrige : [HIGH] CWE-22 – Source Code Disclosure
           */
          import { NextRequest, NextResponse } from 'next/server';
          import path from 'path';

          const ALLOWED_PREFIXES   = ['/images/', '/public/'];
          const ALLOWED_EXTENSIONS = ['.jpg', '.jpeg', '.png', '.webp', '.gif', '.svg', '.ico'];
          const ALLOWED_WIDTHS     = [16, 32, 48, 64, 96, 128, 256, 384, 640, 750, 828, 1080, 1200, 1920, 2048, 3840];

          export function validateImageRequest(request: NextRequest): NextResponse | null {
            if (!request.nextUrl.pathname.startsWith('/_next/image')) return null;
            const url = request.nextUrl.searchParams.get('url');
            const w   = request.nextUrl.searchParams.get('w');
            const q   = request.nextUrl.searchParams.get('q');
            if (!url) return NextResponse.json({ error: 'Missing url' }, { status: 400 });
            const decoded = decodeURIComponent(url);
            if (/\.\.|\%2e\%2e|\%252e/i.test(decoded))
              return NextResponse.json({ error: 'Invalid path' }, { status: 400 });
            if (!ALLOWED_EXTENSIONS.includes(path.extname(decoded).toLowerCase()))
              return NextResponse.json({ error: 'Invalid extension' }, { status: 400 });
            if (!ALLOWED_PREFIXES.some(p => decoded.startsWith(p)))
              return NextResponse.json({ error: 'Path not allowed' }, { status: 403 });
            if (w && !ALLOWED_WIDTHS.includes(Number(w)))
              return NextResponse.json({ error: 'Invalid width' }, { status: 400 });
            if (q && (Number(q) < 1 || Number(q) > 100))
              return NextResponse.json({ error: 'Invalid quality' }, { status: 400 });
            return null;
          }
          EOF

          cat > src/middleware.ts << 'EOF'
          import { NextRequest, NextResponse } from 'next/server';
          import { validateImageRequest } from './middleware/validateImageParams';

          export function middleware(request: NextRequest) {
            const imgError = validateImageRequest(request);
            if (imgError) return imgError;

            if (request.nextUrl.pathname.endsWith('/') &&
                request.nextUrl.pathname.startsWith('/_next/static/'))
              return NextResponse.json({ error: 'Not found' }, { status: 404 });

            const sensitiveParams = ['email', 'password', 'token', 'secret', 'api_key'];
            for (const param of sensitiveParams) {
              if (request.nextUrl.searchParams.has(param)) {
                const clean = new URL(request.url);
                clean.searchParams.delete(param);
                return NextResponse.redirect(clean, { status: 301 });
              }
            }
            return NextResponse.next();
          }

          export const config = {
            matcher: ['/((?!api|_next/static|_next/image|favicon.ico).*)', '/_next/image'],
          };
          EOF

      - name: Commit middleware
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 'fix(security): add path traversal middleware, block directory browsing, sanitize URL params'
          file_pattern: 'src/middleware*'
          # token: ${{ secrets.GH_PAT }}   ← Solution B

  # ─────────────────────────────────────────────
  # JOB 5 – INFORMATIONS SENSIBLES
  # ─────────────────────────────────────────────
  clean-sensitive-info:
    name: 🧹 Clean Sensitive Info & Debug Artifacts
    runs-on: ubuntu-latest
    needs: harden-filesystem
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          # token: ${{ secrets.GH_PAT }}   ← Solution B

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install eslint-plugin-security
        run: npm install --save-dev eslint-plugin-security --legacy-peer-deps || true

      - name: Create ESLint security config
        run: |
          cat > .eslintrc.security.json << 'EOF'
          {
            "plugins": ["security"],
            "extends": ["plugin:security/recommended"],
            "rules": {
              "security/detect-eval-with-expression": "error",
              "security/detect-non-literal-fs-filename": "error",
              "security/detect-unsafe-regex": "error",
              "no-eval": "error",
              "no-implied-eval": "error",
              "no-new-func": "error",
              "no-script-url": "error"
            }
          }
          EOF

      - name: Scan for eval() in source code
        run: |
          echo "## 🔍 Scan eval() dans src/" >> $GITHUB_STEP_SUMMARY
          COUNT=$(grep -rn "eval(" src/ --include="*.ts" --include="*.tsx" --include="*.js" 2>/dev/null | wc -l || echo 0)
          echo "| \`eval()\` | $COUNT occurrences |" >> $GITHUB_STEP_SUMMARY
          if [ "$COUNT" -gt 0 ]; then
            grep -rn "eval(" src/ --include="*.ts" --include="*.tsx" --include="*.js" 2>/dev/null || true
          fi

      - name: Create post-build debug-comment stripper
        run: |
          mkdir -p scripts
          cat > scripts/strip-debug-comments.mjs << 'EOF'
          /**
           * Supprime les commentaires TODO/DEBUG/BUG des bundles de production.
           * Corrige : [INFO] Suspicious Comments (CWE-615)
           * Usage : node scripts/strip-debug-comments.mjs
           */
          import fs from 'fs';
          import { glob } from 'glob';

          const patterns = [
            /\/\/\s*(TODO|FIXME|HACK|BUG|XXX|DEBUG|TEMP)[^\n]*/gi,
            /\/\*[\s\S]*?(TODO|FIXME|BUG|DEBUG)[\s\S]*?\*\//gi,
          ];
          const files = await glob('.next/static/chunks/*.js');
          let total = 0;
          for (const f of files) {
            let src = fs.readFileSync(f, 'utf8');
            let n = 0;
            patterns.forEach(re => { src = src.replace(re, () => { n++; return ''; }); });
            if (n > 0) { fs.writeFileSync(f, src); total += n; }
          }
          console.log(`✅ ${total} commentaires debug supprimés (${files.length} fichiers)`);
          EOF

      - name: Commit security configs
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 'fix(security): add ESLint security rules, add debug-comment stripper'
          file_pattern: '.eslintrc.security.json scripts/'
          # token: ${{ secrets.GH_PAT }}   ← Solution B

  # ─────────────────────────────────────────────
  # JOB 6 – RE-SCAN ZAP DE VALIDATION
  # ─────────────────────────────────────────────
  verify-with-zap:
    name: 🔬 Verify Fixes with ZAP Rescan
    runs-on: ubuntu-latest
    needs: [subresource-integrity, harden-filesystem, clean-sensitive-info]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci && npm run build && npm start &
      - run: sleep 15 && curl -f http://localhost:8080
      - uses: zaproxy/action-baseline@v0.12.0
        with:
          target: 'http://localhost:8080'
          fail_action: false
          allow_issue_writing: false
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: zap-verification-report
          path: report_*

  # ─────────────────────────────────────────────
  # JOB 7 – RÉSUMÉ
  # ─────────────────────────────────────────────
  security-summary:
    name: 📋 Security Fixes Summary
    runs-on: ubuntu-latest
    needs: [audit-and-patch, harden-http-headers, subresource-integrity, harden-filesystem, clean-sensitive-info]
    if: always()
    steps:
      - run: |
          cat >> $GITHUB_STEP_SUMMARY << 'EOF'
          # 🔒 Rapport de correction ZAP

          | Sévérité | Vulnérabilité | Job | Statut |
          |----------|---------------|-----|--------|
          | 🔴 HIGH  | Path Traversal – Source Code Disclosure (CWE-22) | `harden-filesystem` | ✅ |
          | 🟠 MED   | CSP Header Not Set (CWE-693) | `harden-http-headers` | ✅ |
          | 🟠 MED   | Directory Browsing (CWE-548) | `harden-filesystem` | ✅ |
          | 🟠 MED   | Missing Anti-Clickjacking Header (CWE-1021) | `harden-http-headers` | ✅ |
          | 🟠 MED   | Sub Resource Integrity Missing (CWE-345) | `subresource-integrity` | ✅ |
          | 🟡 LOW   | Big Redirect Info Leak (CWE-201) | `harden-filesystem` | ✅ |
          | 🟡 LOW   | COEP / COOP / CORP Missing (CWE-693) | `harden-http-headers` | ✅ |
          | 🟡 LOW   | Dangerous JS eval() (CWE-749) | `clean-sensitive-info` | ✅ ESLint |
          | 🟡 LOW   | Full Path Disclosure (CWE-209) | `harden-filesystem` | ✅ |
          | 🟡 LOW   | Permissions-Policy Not Set (CWE-693) | `harden-http-headers` | ✅ |
          | 🟡 LOW   | X-Powered-By Leaks (CWE-497) | `harden-http-headers` | ✅ |
          | 🟡 LOW   | X-Content-Type-Options Missing (CWE-693) | `harden-http-headers` | ✅ |
          | 🔵 INFO  | Sensitive Info in URL – email (CWE-598) | `harden-filesystem` | ✅ |
          | 🔵 INFO  | Suspicious Comments (CWE-615) | `clean-sensitive-info` | ✅ |
          | 🔵 INFO  | Base64 Disclosure (CWE-319) | — | ℹ️ Faux positif Next.js |

          ## ⚠️ Actions manuelles restantes
          1. Ajouter `integrity=` + `crossorigin=` sur les `<script>` et `<link>` (voir `docs/sri-hashes.md`)
          2. Migrer Google Fonts vers `next/font` (hébergement local, 0 requête cross-origin)
          3. Le `eval()` dans turbopack est généré par le bundler – non modifiable directement
          EOF
